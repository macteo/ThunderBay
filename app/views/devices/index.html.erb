<p id="notice"><%= notice %></p>

<h1>Listing Devices</h1>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Name</th>
      <th>Identifier</th>
      <th>App</th>
      <th>Last access</th>
      <th>Locale</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @devices.each do |device| %>
      <tr>
        <td><%= device.name %></td>
        <td><%= device.identifier %></td>
        <td><%= device.app ? device.app.key : "" %></td>
        <td><%= device.last_access %></td>
        <td><%= device.locale %></td>
        <td><%= link_to 'Send Message', new_message_path(:receiver_id => device.identifier), class: "btn btn-primary" %></td>
        <td>
          <%= link_to 'Show', device, class: "btn btn-default" %>
          <%= link_to 'Edit', edit_device_path(device), class: "btn btn-default" %>
          <%= link_to 'Destroy', device, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger" %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Device', new_device_path, class: "btn btn-primary", :style => "color:#fff !important;" %>
