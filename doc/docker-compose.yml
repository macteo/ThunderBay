db:
  image: postgres
web:
  image: macteo/thunderbay
  links:
    - db
  ports:
    - "80:3000"
  environment:
    SECRET_KEY_BASE: PUT_HERE_YOUR_LONG_SECRET
  command: bundle exec rails s Puma -p 3000 -b '0.0.0.0'
